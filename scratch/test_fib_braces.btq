функция фиб(n) {
    егер n <= 1 {
        қайтару n
    }
    қайтару фиб(n - 1) + фиб(n - 2)
}

жазу("=== Фибоначчи қатары ===")

болсын i = 0
әзірше i <= 10 {
    жазу("fib(", i, ") = ", фиб(i))
    i = i + 1
}
